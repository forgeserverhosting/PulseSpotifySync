name: Build Pulse v0.4 APK

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "app/**"
      - "build.gradle"
      - "settings.gradle"
      - "pulse-test.jks"
      - ".github/workflows/Pulse-v04.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: yes | sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build Pulse v0.4
        run: gradle :app:assembleDebug --stacktrace -PspotifyClientId="${{ secrets.SPOTIFY_CLIENT_ID }}"

      - name: Upload Pulse v0.4 APK
        uses: actions/upload-artifact@v4
        with:
          name: Pulse-v0.4-APK
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
